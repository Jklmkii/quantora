name: CI & Jules PR Verification

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  test:
    name: Run Test Suite & Verification
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies (safe, no lifecycle scripts)
        run: npm ci --ignore-scripts || npm install --ignore-scripts

      - name: Run unit & render tests
        run: npx vitest run

      - name: Verify build bundle
        run: npm run build

      - name: Run static linter
        run: npm run lint

  auto-merge-jules:
    name: Auto Merge Verified Jules PR
    needs: test
    runs-on: ubuntu-latest
    # Security gates:
    # 1. MUST NOT be from an external fork (head repository must match base repository)
    # 2. Author MUST strictly match the verified Jules bot or repo owner (never spoofable substring search)
    if: >
      github.event.pull_request.head.repo.full_name == github.repository &&
      (github.event.pull_request.user.login == 'google-jules[bot]' ||
       github.actor == 'google-jules[bot]' ||
       github.event.pull_request.user.id == 192535081 ||
       github.event.pull_request.user.login == github.repository_owner)
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Approve and merge PR (respecting branch protections)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr review ${{ github.event.pull_request.number }} --repo ${{ github.repository }} --approve --body "Aprovado automaticamente após 100% de aprovação na suíte de testes e verificação estrita de identidade."
          gh pr merge ${{ github.event.pull_request.number }} --repo ${{ github.repository }} --merge
